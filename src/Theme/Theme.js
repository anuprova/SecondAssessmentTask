import { createTheme } from "@mui/material/styles";
const Theme = createTheme({
  palette: { primary: { main: "#127bd7ff" }, secondary: { main: "#f59300ff" } },
  typography: { fontFamily: "Roboto, Arial, sans-serif" },
  
});
export default Theme;